# Leave empty in Vercel so the frontend uses its same-origin /api function.
REACT_APP_BACKEND_URL=

# Required in Vercel for /api/leads.
MONGODB_URI=
MONGODB_DB=datos-ximnanzas
EMERGENT_EMAIL_KEY=
OWNER_EMAIL=
EMAIL_FROM_NAME=XIMNANZAS
EMAIL_REPLY_TO=
